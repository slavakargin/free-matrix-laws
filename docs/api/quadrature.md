# quadrature — brute-force integration

Direct numerical quadrature for the matrix-valued Cauchy transform of
$b \otimes X$, where $b$ is a deterministic matrix and $X$ is a standard
semicircular random variable.

::: free_matrix_laws.quadrature